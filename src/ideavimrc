""" Plugins

set sneak
set surround
set multiple-cursors
set commentary
set argtextobj
set ReplaceWithRegister
set textobj-entire

""" Plugin settings -------------------------

let g:argtextobj_pairs="[:],(:),<:>"

""" Other settings

set showmode
set incsearch
set hlsearch
set highlightedyank

""" Custom mappings

" alternative esc remap experiment
inoremap jk <Esc>

" insert blank line below cursor, stay in normal mode
nnoremap <C-A-o> :set paste<CR>mso<Esc>`s:set nopaste<CR>

""" Custom mappings for JetBrains IDEs

nmap <leader>qq <Action>(QuickJavaDoc)
nmap <leader>qd <Action>(QuickImplementations)
nmap <leader>qf <Action>(Javascript.Linters.EsLint.Fix)
